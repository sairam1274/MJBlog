

<%= form_for([@flit, @flit.comments.build]) do |f| %>
  

  <div class="field">
    <%= f.text_area :comment, rows: 2, class: 'span8' %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn' %>
  </div>
<% end %>

